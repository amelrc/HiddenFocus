import React from 'react';
import data from '../../Data';

const Canada = ({ match }) => {
	console.log(match, 'hello');

	return (
		<>
			<h1>Canada component</h1>
		</>
	);
};

export default Canada;
